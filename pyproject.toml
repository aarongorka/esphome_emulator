[project]
name = "esphome_emulator"
authors = [
  {name = "Aaron Gorka", email = "aarongorka@users.noreply.github.com"},
]
version = "0.0.1"
dependencies = [
  "sh",
  "noiseprotocol",
  "protobuf",
  "zeroconf",
]

[project.scripts]
esphome_emulator = "esphome_emulator.esphome_emulator:run"
